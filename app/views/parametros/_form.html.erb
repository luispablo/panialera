
	<%= form_for(@parametro, validate: true, html: {class: 'form-horizontal'}) do |f| %>
  		<% if @parametro.errors.any? %>
    		<div id="error_explanation">
      			<h2><%= pluralize(@parametro.errors.count, "error") %> prohibited this parametro from being saved:</h2>
      			<ul>
      				<% @parametro.errors.full_messages.each do |msg| %>
        				<li><%= msg %></li>
      				<% end %>
      			</ul>
    		</div>
		<% end %>

  		<div class="control-group">
    		<%= f.label :codigo, 'Código', class: 'control-label' %>
    		<div class="controls"><%= f.text_field :codigo, disabled: true %></div>
    		<%= f.hidden_field :codigo %>
  		</div>
  		<div class="control-group">
    		<%= f.label :nombre, class: 'control-label' %>
    		<div class="controls"><%= f.text_field :nombre %></div>
  		</div>
  		<div class="control-group">
    		<%= f.label :valor, class: 'control-label' %>
    		<div class="controls"><%= f.text_field :valor %>&nbsp;<strong>*</strong></div>
  		</div>
  
  		<div class="form-actions">
    		<%= f.submit 'Grabar', class: 'btn btn-primary' %>
  		</div>
	<% end %>

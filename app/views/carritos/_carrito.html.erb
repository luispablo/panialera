					
	<%= image_tag 'tu_compra.png' %>
	
	<ul class="nav1">
		<%= render(carrito.carrito_items) %>
		<br/>
		Total <%= number_to_currency(carrito.precio_total) %>
	</ul>						

	<%= button_to 'Vaciar carrito', carrito, method: :delete, confirm: '¿Estás seguro?', remote: true %>
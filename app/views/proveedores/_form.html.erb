
	<%= form_for(@proveedor, html: {class: 'form-horizontal'}) do |f| %>
  		<% if @proveedor.errors.any? %>
    		<div id="error_explanation">
      			<h2><%= pluralize(@proveedor.errors.count, "error") %> prohibited this proveedor from being saved:</h2>
				<ul>
      				<% @proveedor.errors.full_messages.each do |msg| %>
        				<li><%= msg %></li>
      				<% end %>
      			</ul>
    		</div>
  		<% end %>
  		
  		<div class="control-group">
    		<%= f.label :nombre, class: 'control-label' %>
    		<div class="controls"><%= f.text_field :nombre %></div>
  		</div>
  		<div class="control-group">
    		<%= f.label :direccion, class: 'control-label' %>
    		<div class="controls"><%= f.text_field :direccion %></div>
  		</div>
  		<div class="control-group">
    		<%= f.label :telefono, class: 'control-label' %>
    		<div class="controls"><%= f.text_field :telefono %></div>
  		</div>
  		<div class="control-group">
    		<%= f.label :email, class: 'control-label' %>
    		<div class="controls"><%= f.text_field :email %></div>
  		</div>
  		<div class="control-group">
    		<%= f.label :pagina_web, class: 'control-label' %>
    		<div class="controls"><%= f.text_field :pagina_web %></div>
  		</div>
  		
  		<div class="form-actions">
    		<%= f.submit 'Grabar', class: 'btn btn-primary' %>
  		</div>
	<% end %>
